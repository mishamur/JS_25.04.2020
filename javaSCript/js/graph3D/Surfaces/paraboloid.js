Surfaces.prototype.paraboloid = (count = 20) =>{
    const points = [];
    const edges = [];
    const polygons = [];

    //print points
    
    for(i = 1; i < count; i++){
        
    }

     return new Subject(points);
}